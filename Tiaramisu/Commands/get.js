'use strict';

const Command = {
	name: `get`,
	aliases: [`mails`, `list`],
	usage: `Use \`<p>get <user_id> <count>\` to get <count> last messages from an user`,
	async run() {
		console.log(`Not implemented yet.`);
	}
};

module.exports = Command;