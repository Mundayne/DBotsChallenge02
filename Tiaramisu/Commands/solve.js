'use strict';

const Command = {
	name: `solve`,
	aliases: [`close`],
	usage: `Use \`<p>solve <user_id> <?message>\` to close a chat and (opcional) reply to the user`,
	async run() {
		console.log(`Not implemented yet.`);
	}
};

module.exports = Command;